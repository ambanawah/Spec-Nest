i hae problems with how this projet was implemented. to push it to international lee i need at least the same options that offers amazon so the models should reflet that. the role based aess was not implemented early enough so we hae to so the art does not feth from the database. i need to inpet and fi the implementation layer by layer to make it high grade database first eommere platform

---

Let me first explore the project structure to understand what's currently implemented.

---

go on

---

Let me read the remaining files properly.

---

Now I have a full picture. Let me break down exactly what's wrong and the layer-by-layer fix plan.

---

Here's the full diagnosis and the plan to make this a high-grade, international-ready platform:

## What's Broken Right Now

**Layer 1 — Database (mostly good, but incomplete)**
- `db/database.sql` is solid: has `exchange_rates`, `categories`, `inventory_logs`, stored procedures, materialized views, `is_admin` flag
- `database.sql` (root) is the old, weak schema — it's what's likely deployed. No roles, no orders, no currencies
- The `users` table has `is_admin` boolean but no proper role system (no `role` enum for guest/customer/staff/admin)
- No `reviews` table, no `wishlists`, no `addresses` table — all needed for Amazon-level features
- No GIN index on `specs_json` for fast filtering

**Layer 2 — Backend API (`src/api/index.js`) — critical issues**
- Zero authentication middleware — cart endpoints take `userId` from the URL with no JWT verification. Anyone can read/modify anyone's cart
- No JWT at all — login returns `userId` in plain JSON, no token
- SQL injection vulnerability in the products filter: `specs_json->>'''${key}'''` — the key is interpolated directly into the query string
- `promotions` endpoint queries a table that doesn't exist in the new schema
- No admin-protected routes
- No `/api/orders` endpoint — the `place_order` stored procedure exists in the DB but is never called
- Cart endpoints use `userId` from URL params instead of from a verified token
- `conversionRates` is an in-memory object — if the server restarts, it's empty until the cron runs, causing broken pricing

**Layer 3 — Frontend — critical issues**
- `CartContext.js` hardcodes `http://localhost:3001` — breaks in production
- `account/page.js` hardcodes `http://localhost:3001` — same issue
- No JWT stored or sent — auth is fake (just stores `userId` in localStorage)
- Homepage "Latest Arrivals" section is 100% hardcoded static HTML — not fetching from DB at all
- Catalog filter sidebar (laptops page) is purely decorative — no state, no API calls
- Pagination is hardcoded static buttons
- No currency selector anywhere in the UI despite the backend supporting it
- No orders page, no order history, no checkout flow
- No reviews, no wishlist, no address management

---

## Fix Plan — Layer by Layer

### Phase 1: Database (foundation)
1. Add `role` column to `users` (`guest`, `customer`, `admin`) replacing the `is_admin` boolean
2. Add missing tables: `reviews`, `wishlists`, `addresses`
3. Add GIN index on `specs_json`
4. Update `place_order` procedure to use role-aware logic

### Phase 2: Backend API (security + completeness)
1. Add JWT auth middleware (`jsonwebtoken` package) — login returns a signed token
2. Protect all cart/order routes with `authenticate` middleware that reads userId from token, not URL
3. Fix the SQL injection in specs filtering
4. Add `POST /api/orders` calling the stored procedure
5. Add `GET /api/orders/:userId` for order history
6. Add admin middleware + admin routes (product CRUD, order status updates)
7. Add `GET /api/reviews/:productId` and `POST /api/reviews`
8. Seed `conversionRates` from DB on startup, not just from external API

### Phase 3: Frontend (connect to real data)
1. Replace hardcoded `http://localhost:3001` with a `NEXT_PUBLIC_API_URL` env variable
2. Store JWT in `httpOnly` cookie or `localStorage`, send as `Authorization: Bearer` header
3. Wire homepage "Latest Arrivals" to `GET /api/products?limit=3`
4. Make catalog filter sidebar functional (price range, specs filters → query params)
5. Add `CurrencySelector` component wired to all product API calls
6. Add checkout page calling `POST /api/orders`
7. Add order history page
8. Add reviews section on product detail page

---

**Where do you want to start?** I recommend Phase 2 (backend security) first since the SQL injection and missing auth are the most critical. I can implement the JWT auth + fix the injection + add the orders endpoint in one pass. Just say the word.

---

phase 1 yes good enough for now there is also the seller aount to implement
phase 2 jwt late not now remind me, build as endpoint doument that for all endpoints. prie seletor should authomatially update using the latest ehaange rates add afrian urrenies
phase 3 jwt later, 

ok update that and proeed with phase  1

---

Let me check the current DB setup script before making changes.

---

Good. Now I have everything I need. Here's the full plan before writing:

**Phase 1 changes:**
- `db/database.sql` — full schema rewrite with: `role` enum (guest/customer/seller/admin), `addresses`, `reviews`, `wishlists`, GIN index on `specs_json`, updated `place_order` to log inventory with role context, seller-specific fields on `products`
- `src/api/index.js` — fix SQL injection, add `/api/orders` endpoint, add African + major currencies to seed data, auto-refresh rates from DB on startup, add all missing endpoints, write endpoint documentation in comments
- `db/setup-db.js` — point to `db/database.sql` not root `database.sql`

Let's go.

---

Full schema rewrite with roles, seller accounts, reviews, wishlists, addresses, GIN index, African currencies, and updated stored procedures.

---

Now update the setup script to read from the correct file and add a path fix:

---

Update setup script to read from db/database.sql and resolve path relative to the script location.

---

Now rewrite the backend API — fix SQL injection, add all endpoints, auto-seed rates from DB, add seller routes, document everything:

---

go on

---

yes

---

Let me write the full backend API now.

---

stop again go on

---

Full backend API rewrite with all endpoints documented, SQL injection fixed, African currencies, auto-refresh rates from DB, seller routes, orders endpoint calling stored procedure.